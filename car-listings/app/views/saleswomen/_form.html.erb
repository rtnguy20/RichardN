<%= form_for(saleswoman) do |f| %>
  <% if saleswoman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saleswoman.errors.count, "error") %> prohibited this saleswoman from being saved:</h2>

      <ul>
      <% saleswoman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fullname %>
    <%= f.text_field :fullname %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :profile_image_url %>
    <%= f.text_field :profile_image_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
